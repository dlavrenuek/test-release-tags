name: Add a random question

on:
  issues:
    types:
      - opened

jobs:
  add-random-question:
    runs-on: ubuntu-latest
    steps:

      - name: debug
        run: echo "${{ toJson(github.event) }}"

